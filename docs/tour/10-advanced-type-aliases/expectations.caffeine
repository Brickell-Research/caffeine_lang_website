_common (Provides): { window_in_days: 30, status: true }

Expectations measured by "api_availability"
  * "checkout_availability" extends [_common]:
    Provides {
      # env is default value here
      threshold: 99.95%
    }

  * "staging_checkout" extends [_common]:
    Provides {
       env: "staging", 
       threshold: 95.0% 
      }
